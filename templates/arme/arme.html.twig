{% extends 'base.html.twig' %}

{% block title %}Arme{% endblock %}

{% block monTitre %}Mon {{ arme.nom }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-2">
            <img src="{{ asset('images/armes/' ~ arme.nom | lower | replace({'é':'e'}) ~ '.png') }}">
        </div>
        <div class="col">
                <div>{{ arme.nom }}</div>
                <div>{{ arme.description }}</div>
                <div>{{ arme.degat }}</div>
        </div>
    </div>
{% endblock %}