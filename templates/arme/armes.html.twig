{% extends 'base.html.twig' %}

{% block title %}Armes{% endblock %}

{% block monTitre %}Liste des armes{% endblock %}

{% block body %}
    {% for weapon in weapons %}
        <div class="row">
            <div class="col-2">
                <img src="{{ asset('images/armes/' ~ weapon.nom | lower | replace({'é':'e'}) ~ '.png') }}">
            </div>
            <div class="col">
                <a href="{{ path("afficher_arme", {'nom': weapon.nom | lower | replace({'é':'e'}) }) }}">{{ weapon.nom }}</a></td>
            </div>
        </div>
    {% endfor %}
    
{% endblock %}